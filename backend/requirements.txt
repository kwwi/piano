# Base backend (API + job orchestration + MIDI->MusicXML). Light + always installable.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
celery>=5.4
redis>=5.0
music21>=9.1
pretty_midi>=0.2.10
soundfile>=0.12
numpy>=1.26
httpx>=0.27
pytest>=8.0

# Optional: server-side Verovio rendering (also used to validate generated MusicXML)
verovio>=4.3
