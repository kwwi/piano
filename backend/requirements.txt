# Base backend (API + job orchestration + MIDI->MusicXML). Light + always installable.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
celery>=5.4
redis>=5.0
music21>=9.1
pretty_midi>=0.2.10
soundfile>=0.12
numpy>=1.26
httpx>=0.27
pytest>=8.0
Pillow>=10.0
pytesseract>=0.3.10
opencv-python-headless>=4.8
scikit-learn>=1.3
joblib>=1.3
librosa>=0.10
jianpu-ly>=1.8

# Optional GPL beat tracker (used automatically when importable):
#   pip install aubio

# Optional: server-side Verovio rendering (also used to validate generated MusicXML)
verovio>=4.3

# System packages:
#   tesseract (+ chi_sim) for /omr
#   ffmpeg for audio/video extract
#   lilypond (optional) if compiling jianpu-ly .ly output
