name: jianpu_core
description: "Pure-Dart engine for numbered notation (jianpu): DSL parsing, an editable IR, pitch resolution, and MusicXML/MIDI generation. No Flutter dependency."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
